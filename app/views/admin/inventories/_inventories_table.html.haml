#inventories-table
  %table.table.table-hover.no-margin
    %thead
      %tr
        %th
          ID
        %th
          Company ID
        %th
          Quantity
      %tbody
        = render @inventories